const signUpView = () => {
  const current_view = "signup";
  return current_view;
};

const loginView = () => {
  const current_view = "login";
  return current_view;
};

module.exports = {
  signUpView,
  loginView,
};
